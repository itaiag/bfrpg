# BFRPG Project — Claude Memory

## Project Overview
Quarto book site for Basic Fantasy RPG (BFRPG) rules. English source at root, Hebrew translation in `he/`. Built output goes to `docs/` (GitHub Pages).

## Translation System
All translator code lives in `translator/`. Key CLI commands:
```
node translator/translate.js <file.qmd>   # translate one or more files
node translator/translate.js --all        # translate all content files
node translator/translate.js --dry-run <file>  # preview segments, no API
node translator/translate.js --patch      # re-apply post-processors to he/ files
node translator/translate.js --build      # generate he/_quarto.yml + quarto render he/
```

## Build Commands
```
quarto render          # English site → docs/
quarto render he/      # Hebrew site → docs/he/  (NOT --profile he)
```
**Important**: `--profile he` merges chapters additively with the root `_quarto.yml`. Always render `he/` as a standalone project instead.

## Architecture Decisions

### Hebrew Quarto Build
- `he/` is its own standalone Quarto project
- `he/_quarto.yml` is auto-generated by `--build` (gitignored)
- Uses `lang: he-IL` (not `lang: he` — conflicts with `he/` directory name)
- `output-dir: ../docs/he`

### Dice Notation
- Keep English `d` notation (1d6, 2d8, etc.) — do NOT use Hebrew ק
- Reason: ק is RTL; bidi algorithm scrambles surrounding digits in Hebrew paragraphs
- Hebrew RPG publications standardly use English `d` notation

### Unit Conversion
- Imperial units are **replaced entirely** with metric (no parenthetical append)
- Game-friendly rounding: feet×0.3 (→ nearest 0.5m), miles×1.5 (→ nearest 0.5km), pounds×0.45 (→ nearest 1kg)
- Post-processor handles conversion; AI is told NOT to convert units
- Hebrew transliterations (פיט, רגל, מייל, פאונד) also converted

### AI Translation
- Default model: `gpt-4o` (configurable via `OPENAI_MODEL` env var)
- API key in `.env` (gitignored); template at `.env.example`
- Segments batched ~3000 tokens per call

### GM Gender
- Game Master (מנחה) is always feminine in Hebrew

### Path Fixing
- All `he/*.qmd` files are one level deeper than root
- `fixRelativePaths()` handles: JS imports, FileAttachment, markdown `(./`, HTML src/href
- Applied to TRANSLATE, PRESERVE, and OJS segments

## Key Files
| File | Purpose |
|------|---------|
| `translator/glossary.json` | User-editable RPG term translations |
| `translator/config.js` | System prompt, file lists, book structure |
| `translator/parser.js` | QMD segmenter (PRESERVE/TRANSLATE/OJS) |
| `translator/unit-converter.js` | Imperial→metric post-processor |
| `translator/openai-client.js` | GPT-4o-mini API wrapper |
| `translator/lang-switch.js` | Client-side EN↔HE toggle button |
| `custom-rtl.css` | RTL layout, Frank Ruhl Libre font |
| `he/` | Translated QMD files (git-tracked) |
| `docs/he/` | Built Hebrew HTML site (git-tracked for Pages) |

## Gitignore Notes
- `.env` — never commit (API key)
- `he/_quarto.yml` — auto-generated, gitignored
- `he/.quarto/` — Quarto cache, gitignored
- `node_modules/` — gitignored
- `he/` QMD files — tracked
- `docs/he/` — tracked (GitHub Pages)
